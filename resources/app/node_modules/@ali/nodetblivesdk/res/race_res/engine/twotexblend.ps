#ifdef GL_ES
precision highp float;
#endif
uniform sampler2D 	tex0;
uniform sampler2D 	tex1;
varying vec2 UV0;
varying vec2 UV1;
void main()
{
	vec4 vColor = texture2D(tex0, UV0);
	vec4 vAlpha = texture2D(tex1, UV1);
	gl_FragColor = vec4(vColor.rgb, 1.0 - vAlpha.r);
}