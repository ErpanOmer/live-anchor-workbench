#ifdef GL_ES
precision highp float;
#endif
attribute vec4 aVertexCo;
attribute vec2 aTextureCo;

varying vec2 UV;
void main()
{
	vec4 vPos = vec4(aVertexCo.xyz, 1.0);
	UV = aTextureCo;
	gl_Position = vPos;
}
