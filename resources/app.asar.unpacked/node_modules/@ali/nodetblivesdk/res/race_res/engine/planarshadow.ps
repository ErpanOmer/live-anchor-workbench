precision highp float;
uniform sampler2D 	tex0;
varying vec3 UV;
void main()
{
	vec4 vColor = texture2D(tex0, UV.xy);
	if(vColor.a * UV.z < 0.5)
	{
		discard;
	}
	gl_FragColor = vec4(0.0, 0.0, 0.0,0.5);
}