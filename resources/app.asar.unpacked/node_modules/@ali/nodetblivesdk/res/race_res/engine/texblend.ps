#ifdef GL_ES
precision highp float;
#endif
uniform sampler2D 	tex0;
uniform sampler2D 	tex1;
varying vec2 UV;
uniform float alpha;
void main()
{
	vec4 vColor0 = texture2D(tex0, UV);
	vec4 vColor1 = texture2D(tex1, UV);
	gl_FragColor = mix(vColor0, vColor1, alpha);
}