#ifdef GL_ES
precision highp float;
#endif
uniform sampler2D 	tex0;
uniform float		fColorScale;
varying vec2 UV;
varying vec4 vColor;

void main()
{
	gl_FragColor = texture2D(tex0, UV) * vColor;
	gl_FragColor.rgb = gl_FragColor.rgb * vec3(fColorScale);
}